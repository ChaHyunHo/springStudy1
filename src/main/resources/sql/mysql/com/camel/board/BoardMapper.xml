<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.camel.board.mapper.BoardMapper">
	
	<select id="boardDetail" resultType="com.camel.board.mapper.vo.BoardVO">
		SELECT * FROM board WHERE NO = #{no}
	</select>
 
</mapper>
